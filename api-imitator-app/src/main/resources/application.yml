# MySQL 配置 (本地部署)
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/api_mock?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    username: root
    password: cj@456789Xx..
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false

# 服务端口
server:
  port: 8080
